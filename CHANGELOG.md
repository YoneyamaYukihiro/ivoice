# 変更履歴

このファイルは voice-reader（AI-Voice司会くん）の変更履歴です。
形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) を採用します。

## [Unreleased]

---

## [0.4.0] - 2026-06-18

ローカル LLM 連携リリース。固定原稿を読むだけの「BOT」から、ネタを渡すと原稿を組み立てる相棒へ。

### 追加
- **原稿ジェネレーター（ローカル Ollama）**：箇条書き・アジェンダ・Teams チャットを渡すと司会原稿を自動生成
  - `/reader` 上部の「✨ 原稿を作る」パネルから利用。会議名・トーン（標準／フレンドリー／フォーマル）・見出し付与を選べる
  - 生成結果は `# 見出し` 付きで「読み上げるテキスト」に反映され、そのまま司会モードで再生できる
  - メンバー名簿を LLM に渡して氏名表記を統一。敬称は従来どおり後段の `applyMembers` が付与
  - サーバ側 `/api/reader/script` が `http://localhost:11434` の Ollama を中継（CORS 回避）。既定モデルは `qwen2.5:3b`
  - `.env` の `OLLAMA_BASE_URL` / `OLLAMA_MODEL` で接続先・既定モデルを切替可能
  - **モデル選択ドロップダウン**：インストール済みモデル（`/api/reader/models` で `/api/tags` を取得）をパネルから選んで生成。リクエストごとに `OLLAMA_MODEL` を上書き
  - Ollama 未起動・モデル未 pull は画面にメッセージ表示し、既存機能には影響しない（クラウド非依存・無料・キー不要を維持）
  - 後処理で前置き（「はい、承知しました」等）・コードフェンス・リテラル `\n` を除去
  - **創作・脱落の抑制**：プロンプトで「全項目を漏れなく反映」「書いていない事実・固有名詞・数字を追加しない」を最優先指示にし、`temperature` を 0.6 → 0.3（`top_p` 0.8）に下げてネタへの忠実度を向上
- プロジェクト直下に `start.bat` と `stop.bat` を追加（ダブルクリックで起動・停止）
  - `start.bat` は **完全非表示** で dev サーバを起動し、5 秒後にブラウザを自動で `/reader` に開く
  - 出力は `dev-server.log` に記録
  - 停止は `stop.bat` から（Ctrl+C は使えない）

---

## [0.3.0] - 2026-06-05

データ管理の機能拡張リリース。辞書・メンバー・テンプレすべてに編集とバックアップを追加。

### 追加（Added）
- 辞書（`/reader/dictionary`）：表記・よみがなを **インライン編集**できるように
- メンバー（`/reader/members`）：氏名・よみがな・敬称を **インライン編集**できるように
- 辞書・メンバー・テンプレの 3 種すべてに JSON **エクスポート / インポート** 機能
  - インポート時は既存データを上書き（確認ダイアログあり）、id を再採番
  - エクスポートファイル名：`voice-reader-{dictionary|members|templates}-YYYY-MM-DD.json`
  - 別 PC や別ブラウザへのデータ移行・バックアップに対応

### 修正（Fixed）
- `{presenter}` `{担当者}` で選んだ人物の名前が、`{本日の予定}` 等の自由文中に出てきても **敬称が付かないように**
  - その日の発声中だけ、選択者を `isSelf` 扱いとしてメンバー敬称付与から除外
  - members の永続データには影響なし

### 変更（Changed）
- エクスポートボタンのラベルから「(JSON)」を削除（ダウンロード時のファイル拡張子で形式が分かるため）

---

## [0.2.0] - 2026-06-05

`/reader` の運用性を高めるアップデート。プレースホルダ周りを中心に拡張。

### 追加（Added）
- textarea プレースホルダの判定に `一言` `コメント` `comment` を追加
  - `{一人一言}` `{コメント}` 等も複数行入力欄として表示される
- textarea プレースホルダ（`{本日の予定}` 等）に「クリア」ボタンを追加
- textarea プレースホルダにも「チャットを整形」ボタンを追加（メイン textarea と同じ整形ロジック）
- セクション一覧の下に「実際に読み上げられる全文プレビュー」を折りたたみで追加
  - 辞書置換・句読点正規化まで適用済みの最終形をセクション単位で表示
  - セクション一覧は引き続き原稿に近い形を保持

### 変更（Changed）
- プレースホルダが空欄でも再生できるように
  - 未入力プレースホルダは無音で飛ばされる（旧: 再生ボタンを無効化）
  - 本文が空になったセクションは丸ごとスキップされる
- UI ラベルの「Copilot 出力」を「チャット」に変更（Copilot / ChatGPT 等の汎用名に）
  - メイン textarea のボタン: 「チャットを整形」
  - textarea プレースホルダのボタン: 「チャットを整形」「クリア」

---

## [0.1.0] - 2026-06-05

最初のリリース。dog-food として運用可能な状態。`/reader` で日本語の文章読み上げと朝会司会モードが動作する。

### 追加（Added）

#### 読み上げの基本
- テキストを貼って Web Speech API で読み上げ（`/reader`）
- 声の選択、話速調整、一時停止／再開／停止
- 読み上げ前の自動正規化：（） ：／ → 「、」、 「」『』【】 → 削除

#### 司会モード
- `# 見出し`（全角 `＃` も可）でテキストをセクション分け
- セクション間に間（デフォルト 1 秒、0〜15 秒で調整可）
- 待機中の「次のセクションへ」で早送り
- セクション切り替え時に最新の話速・声を反映

#### 固有名詞辞書（`/reader/dictionary`）
- 表記 → よみがな の登録・編集・削除
- 全置換（長い表記優先）

#### メンバー名簿（`/reader/members`）
- 氏名・よみがな・敬称（さん／くん／様／なし）
- 「自分」フラグ：敬称を完全に剥がす（1 人のみ）
- CSV 一括追加
- 本文に既に敬称があれば優先（重複防止）

#### 台本テンプレ
- localStorage に名前付きで保存・呼び出し・上書き
- プルダウンから即切替

#### プレースホルダ
- `{date}` `{today}` `{date_full}` `{weekday}` `{time}` の自動入力
- `{presenter}` `{担当者}` でメンバーから選択（未登録時は手動入力）
- `{本日の予定}` `{明日の予定}` 等の複数行 textarea プレースホルダ
- 未入力検出で再生ボタンを無効化

#### バージョン表示
- 画面ヘッダにアプリバージョンを表示（`package.json` の `version` を参照）

#### Copilot 出力整形ボタン
- `**太字**` `## 見出し` `＃見出し` `- 箇条書き` の正規化
- 空行で囲まれた短い行を `# 見出し` として検出

### 設定（Configuration）

- ブラウザタブタイトル `AI-Voice司会くん`
- 話速デフォルト 1.1x（範囲 0.5x〜1.5x）
- セクション間の間デフォルト 1 秒

### ドキュメント（Documentation）

- `docs/voice-reader/PLAN.md` — 企画書
- `docs/voice-reader/SPEC.md` — 技術仕様
- `docs/voice-reader/USAGE.md` — 使い方ガイド

### 既知の制約

- mp3 / 音声ファイル出力は不可（Web Speech API の制約）
- データはブラウザの localStorage に保存。デバイス間共有・バックアップ機能は未実装
- Teams 会議への音声配信は未対応（Phase 4 で別エンジン検討予定）

---

## バージョン付与のルール

- **メジャー（X.0.0）**: 既存テンプレ・辞書・メンバーが壊れる仕様変更（localStorage キー変更等）
- **マイナー（0.X.0）**: 後方互換ある新機能追加
- **パッチ（0.0.X）**: バグ修正のみ、機能追加なし
